# Exclude short-form videos by duration

ZTube excludes every video whose YouTube duration is three minutes or less, including Shorts discovered inside an approved channel or playlist. This deliberately removes some ordinary short videos as well, in exchange for a simple, fast rule based entirely on YouTube Data API metadata with no additional classification request.
